log/

# colcon 산출물 — 47차 P0~P2에서 처음으로 colcon 빌드가 서면서 생겼다.
# 재생성 가능한 바이너리라 커밋하지 않는다.
build/
install/

# Python 캐시 — 47차에 실수로 추적됐다(launch·pytest가 만든다).
__pycache__/
*.pyc

# 검사기 스파이크가 남기는 방출물 — 재생성 가능하고 「그때의 기록」도 아니다.
emitted_*.json
