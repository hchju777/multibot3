# sadg.yaml — mrs_sadg 노드 파라미터(sadg_t0_node · switch_selector_node).
#
# 🔴 오늘 두 노드 모두 ROS 파라미터를 하나도 선언하지 않는다([실물] grep
# "declare_parameter" src/node/*.cpp → 0건). 이 파일은 CN-6 자리를 예약만
# 한다 — 채울 키가 아직 없다. 이유: 두 노드가 축소 배선 상태라서다
# (21_integration.md§6-3) — service::ExecutionTickService의 매틱 오케스트레이션
# (릴리스 비트·승급 rung·EVAC 검출)이 이번 라운드도 배선되지 않았고, 그것을
# 배선하는 라운드가 서면 이 파일에 method.modules.sadg.*가 채워진다.
#
# 값을 지어내 여기 채우지 않는다 — 소비할 코드가 없는 config 키는 죽은 키다.
